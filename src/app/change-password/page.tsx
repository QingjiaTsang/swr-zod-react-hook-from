import React, { FC } from 'react';
import ChangePswCard from '@/components/ChangePswCard';

type TProps = {};
const ChangePasswordPage: FC<TProps> = (props) => {
  return <ChangePswCard />;
};
export default ChangePasswordPage;
